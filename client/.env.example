# Frontend (Vercel) environment variables
# URL of the deployed backend API (no trailing slash).
# In development this is ignored and the Vite dev proxy (/api) is used.
VITE_API_URL=https://your-backend-url.onrender.com
